// import {expect, test} from '@oclif/test'

describe('runner', () => {
  // test
  // .stdout()
  // .command(['runner', 'friend', '--from=oclif'])
  // .it('runs hello cmd', ctx => {
  //   expect(ctx.stdout).to.contain('hello friend from oclif!')
  // });

})
